@AGENTS.md

# Claude Code overlay

- Use agent teams or subagents only for bounded, independent work that benefits from
  parallelism or context isolation.
- Keep teammate reports concise and synthesize their results in the primary session.
- Override the shared attribution footer: when posting a GitHub comment, review, or
  reply as the user, end with the following footer instead, never both:
  `Sent from Claude Code session`.
